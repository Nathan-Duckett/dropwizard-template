environment: ${ENVIRONMENT:-dev}

logging:
  appenders:
    - type: console
      layout:
        type: json

server:
  enableVirtualThreads: true
  requestLog:
    appenders:
      - type: console
        layout:
          type: access-json